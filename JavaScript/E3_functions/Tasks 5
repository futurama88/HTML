const power = (x, n) => x ** n;

Вызов функции
const result = power(8, 3);
console.log(result);
512

const power = (x, n) => x ** n;

Вызов функции
const result = power(15, 3);
console.log(result);
3375

const power = (x, n) => x ** n;

Вызов функции
const result = power(125, 3);
console.log(result);
1953125